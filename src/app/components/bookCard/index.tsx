import React from "react";
import styled from "styled-components";
import tw from "twin.macro";

const CardContainer = styled.div`
  ${tw`flex items-center justify-center px-2 py-1 bg-white rounded-md`}
`